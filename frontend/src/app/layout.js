import "./globals.css";
import "../components/navbar_out.js";
import Navbar from "../components/navbar_out.js";

export const metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <head>
        <title>EduNova</title>
      </head>
      <body>
        <main>{children}</main>
      </body>
    </html>
  );
}
